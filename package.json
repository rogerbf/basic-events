{
  "name": "basic-events",
  "version": "0.0.2",
  "description": "A very basic event emitter",
  "main": "dist/index.js",
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "prebuild": "rimraf dist",
    "build": "babel --out-dir dist src",
    "prepublish": "npm run build"
  },
  "eslintConfig": {
    "env": {
      "node": true
    },
    "parserOptions": {
      "ecmaVersion": 6
    }
  },
  "babel": {
    "presets": [
      "es2015"
    ]
  },
  "keywords": ["event", "events", "eventemitter", "emitter", "pubsub"],
  "author": "rogerbf <roger@bergfalk.nu> (http://bergfalk.nu/)",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "6.10.1",
    "babel-preset-es2015": "6.9.0",
    "rimraf": "2.5.3"
  }
}
